{
  "name": "fornax-edge",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
