# GA4 web stream Measurement ID for fornax.horo.run (not a secret — see
# src/analytics.ts). Analytics no-ops entirely when this is unset, so local
# dev and PR preview builds work fine without it.
VITE_GA_MEASUREMENT_ID=
